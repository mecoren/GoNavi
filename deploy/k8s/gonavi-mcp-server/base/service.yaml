apiVersion: v1
kind: Service
metadata:
  name: gonavi-mcp-server
  namespace: gonavi
  labels:
    app.kubernetes.io/name: gonavi-mcp-server
spec:
  selector:
    app.kubernetes.io/name: gonavi-mcp-server
  ports:
    - name: http
      port: 8765
      targetPort: http
      protocol: TCP
